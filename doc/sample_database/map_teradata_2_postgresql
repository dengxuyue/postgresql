                  How to convert a Teradata database to a PostgreSQL database

1) The features that PostgreSQL does not support.

1.1) Title
1.2) Format
1.3) Casespecific

2) The features that PostgreSQL supports in a different way.

2.1) Unique primary index(UPI)
In fact, the UPI is defined on the table's primary key.
So we can directly map UPI to primary key.

2.2) Non-unique primary index(UPI)
We map NUPI to an non-unique index.

